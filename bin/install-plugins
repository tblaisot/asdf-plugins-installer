#!/usr/bin/env bash

FILE="$HOME/.asdf-plugins"
if [[ -f "$FILE" ]]; then
    while IFS=$' ' read -r pluginname pluginsrc; do
        asdf plugin add "${pluginname}" "${pluginsrc}"
    done < "$FILE"
else
    touch "$FILE"
fi


